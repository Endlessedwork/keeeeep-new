# 🔑 วิธีตั้งค่า OpenAI API Key

## ทำไมต้องมี API Key?

แอป Keeeeep ใช้ OpenAI GPT-4o ในการสรุปเนื้อหาเว็บไซต์เป็นภาษาไทยอัตโนมัติ เมื่อคุณเอาแอปขึ้น Play Store ผู้ใช้งานจะต้องใช้ API key ของคุณเพื่อใช้ฟีเจอร์นี้

## 📋 ขั้นตอนการสมัคร OpenAI API

### 1. สมัครบัญชี OpenAI
- ไปที่: https://platform.openai.com
- คลิก "Sign up" และสร้างบัญชี (ใช้ Google/Email ก็ได้)

### 2. เติมเงินเข้าบัญชี
- ไปที่: https://platform.openai.com/settings/organization/billing/overview
- คลิก "Add payment method"
- เติมเงินอย่างน้อย $5 (ประมาณ 170 บาท)
- **หมายเหตุ**: ตั้ง Usage Limit ได้ เช่น $10/เดือน เพื่อป้องกันค่าใช้จ่ายเกิน

### 3. สร้าง API Key
- ไปที่: https://platform.openai.com/api-keys
- คลิก "Create new secret key"
- ตั้งชื่อ เช่น "Keeeeep App"
- **⚠️ สำคัญ**: Copy key ทันที (จะไม่แสดงอีก)
- key จะขึ้นต้นด้วย `sk-proj-...`

### 4. ใส่ API Key ในโปรเจค

เปิดไฟล์ `.env` และแก้ไขบรรทัดนี้:

```
EXPO_PUBLIC_OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxx
```

แทนที่ `sk-proj-xxxxxxxxxxxxxxxxxx` ด้วย API key จริงของคุณ

### 5. Restart แอป
- กด Ctrl+C ใน terminal (ถ้ามีแอปรันอยู่)
- รันใหม่: `bun start`

## 💰 ค่าใช้จ่าย

### ราคา GPT-4o (ณ มกราคม 2025)
- **Input**: $2.50 ต่อ 1 ล้าน tokens
- **Output**: $10.00 ต่อ 1 ล้าน tokens

### การใช้งานจริง
- การสรุป 1 เว็บไซต์ = ประมาณ **$0.01-0.05** (0.35-1.70 บาท)
- ถ้ามีผู้ใช้ 100 คน สรุปวันละ 5 bookmarks = **$25-250/เดือน**

### การประหยัดค่าใช้จ่าย

#### 1. ตั้ง Usage Limit
- ไปที่ Billing settings
- ตั้ง Hard limit เช่น $10/เดือน

#### 2. ใช้โมเดลที่ถูกกว่า
แก้ไขในไฟล์ `src/api/chat-service.ts` บรรทัด 75:

```typescript
const defaultModel = "gpt-4o-mini"; // ถูกกว่า gpt-4o ถึง 10 เท่า
```

**ราคา gpt-4o-mini**: $0.15/$0.60 ต่อล้าน tokens (ถูกกว่า 90%)

#### 3. จำกัดจำนวนครั้งการสรุป
เพิ่มระบบ rate limiting ในแอป เช่น:
- สรุปได้แค่ 10 ครั้งต่อวัน
- หรือแสดง cached summary ถ้าเคยสรุป URL นี้แล้ว

## 🔒 ความปลอดภัย

### ⚠️ ข้อควรระวัง
- **อย่าแชร์ API key ให้ใคร**
- **อย่า commit .env ขึ้น Git** (มี .gitignore ป้องกันอยู่แล้ว)
- **API key ฝังในแอป** = ใครก็เจาะดูได้ (ใช้ limit ป้องกัน)

### วิธีป้องกัน

#### 1. ตั้ง Rate Limit ใน OpenAI Dashboard
- ไปที่ API key settings
- ตั้ง "Rate limits" เช่น:
  - 100 requests/day
  - 1000 requests/month

#### 2. ตั้ง Usage Limits
- Hard limit: $10/เดือน
- Email notification: เมื่อใช้ไป 80%

#### 3. Monitor การใช้งาน
- เช็คที่: https://platform.openai.com/usage
- ดูว่ามีการใช้ผิดปกติหรือไม่

## 🚀 ทางเลือกอื่นๆ (ถ้าไม่อยากจ่ายเงิน)

### 1. ใช้ Google Gemini API (ฟรี)
- Free tier: 15 requests/นาที
- แต่คุณภาพสรุปภาษาไทยอาจไม่ดีเท่า GPT-4o

### 2. ปิดฟีเจอร์ AI
- ลบปุ่ม "สรุปด้วย AI"
- ให้ผู้ใช้เขียนสรุปเอง
- แอปยังใช้งานได้ปกติ (แค่ไม่มีสรุปอัตโนมัติ)

## ❓ คำถามที่พบบ่อย

### Q: ต้องใส่บัตรเครดิตเหรอ?
A: ใช่ครับ OpenAI ต้องเติมเงินก่อนถึงจะใช้ API ได้

### Q: ถ้า API key หมดเงินจะเกิดอะไรขึ้น?
A: แอปจะแสดง error message และผู้ใช้จะสรุปไม่ได้ (ฟีเจอร์อื่นใช้งานได้ปกติ)

### Q: มีวิธีให้ผู้ใช้ใส่ API key เองได้ไหม?
A: ได้ครับ แต่ต้องแก้โค้ดเพิ่ม (ให้ผู้ใช้แต่ละคนใส่ key ของตัวเองในแอป)

### Q: ค่าใช้จ่ายจริงๆ ประมาณเท่าไหร่?
A: ขึ้นกับจำนวนผู้ใช้:
- 10 users × 5 bookmarks/day = ~$2-5/เดือน
- 100 users × 5 bookmarks/day = ~$25-50/เดือน
- 1000 users × 5 bookmarks/day = ~$250-500/เดือน

## 📞 ติดปัญหา?

ถ้ามีปัญหาหรือต้องการความช่วยเหลือ:
- Email: endlessedwork@gmail.com
- OpenAI Support: https://help.openai.com

---

✅ **เมื่อตั้งค่าเสร็จแล้ว ฟีเจอร์ AI สรุปจะทำงานได้ปกติ!**
